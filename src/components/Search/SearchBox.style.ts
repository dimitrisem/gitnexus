import { makeStyles } from '@mui/styles'

const SearchBoxStyle = makeStyles( () => ( {
  root: {
    position: 'absolute',
    bottom:   '35px',
    margin:   '0 auto',
    left:     '0',
    right:    '0',
    width:    'fit-content',
  }
} ) );
	
export default SearchBoxStyle;
